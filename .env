# Copy this file to `.env` and fill in your values.
# Never commit `.env` itself — it's in .gitignore.

# Where the data folder lives. Should point to a shared drive in normal use,
# or a local path during development.
DATA_ROOT="/Users/loucas/Documents/ORG/things too big for git/DISTRICT"

# Where your local clone of the geo-catalogue repo lives.
CATALOGUE_PATH=/Users/loucas/Documents/ORG/github/data-catalogue/geo-catalogue

# Optional: log level for ingest and cleaning scripts.
# One of DEBUG, INFO, WARNING, ERROR.
LOG_LEVEL=INFO
